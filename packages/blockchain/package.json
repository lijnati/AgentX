{
  "name": "@agentx/blockchain",
  "version": "0.1.0",
  "private": true,
  "description": "BNB Smart Chain configuration and blockchain primitives for AgentX",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@agentx/domain": "*",
    "@agentx/validation": "*",
    "viem": "^2.23.27"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "typescript": "^5.8.2"
  }
}
