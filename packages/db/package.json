{
  "name": "@agentx/db",
  "version": "0.1.0",
  "private": true,
  "description": "Database client and Prisma schema for AgentX",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "generate": "prisma generate",
    "build": "tsc -b",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist *.tsbuildinfo src/generated"
  },
  "dependencies": {
    "@prisma/client": "^6.4.1"
  },
  "devDependencies": {
    "prisma": "^6.4.1",
    "rimraf": "^6.0.1",
    "typescript": "^5.8.2"
  }
}
