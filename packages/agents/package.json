{
  "name": "@agentx/agents",
  "version": "0.1.0",
  "private": true,
  "description": "Agent discovery, normalization, and execution interfaces for AgentX",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@agentx/blockchain": "*",
    "@agentx/domain": "*",
    "@agentx/validation": "*"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "typescript": "^5.8.2"
  }
}
